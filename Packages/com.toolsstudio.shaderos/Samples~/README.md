This folder is intentionally empty in this release.

ShaderOS analyzes the materials and shaders already present in the project it
is installed into, and ships its own synthetic walkthrough — the "Load Demo
Data" action available from every tab — instead of a separate sample scene.
The `~` suffix keeps this folder hidden from Unity's Asset Database until it
is genuinely needed.
